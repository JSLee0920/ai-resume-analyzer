# Copy to .env and fill in. .env is gitignored.
# Used by docker-compose.yml for the local Postgres container.

POSTGRES_DB=ResumeAnalyzerDb
POSTGRES_USER=admin
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=5433
